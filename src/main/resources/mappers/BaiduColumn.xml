<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ant.model.BaiDuColumn">

	<!-- 新增 -->
	<insert id="create" parameterType="aliasesBaiDuColumn">
		insert into baiducolumn
		(columnName,createdate)
		values
		(#{columnName},#{createdate})
	</insert>
</mapper>